# Sensor polygon visualization (circuit)

The followed car renders its sensor coverage as a translucent light-blue
polygon. The polygon uses the existing sensor origin and ray endpoints,
including obstacle-shortened endpoints, with a subtle matching outline. Ray
casting, readings, neural inputs, and visibility rules are unchanged.
