# Circuit panel layout

Follow controls use a four-column grid. The explanatory text and score legend
sit side by side in independently scrollable panels. The neural-network canvas
has a neutral light-gray outline by default; selecting a numbered brain colors
that outline with the stable brain-group color. The `all` and mixed selections
keep both their buttons and canvas outline light gray, avoiding per-frame DOM
color updates while mixed selection colors change.
