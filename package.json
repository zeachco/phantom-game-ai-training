{
  "name": "nodium-ai",
  "version": "1.0.0",
  "author": "Olivier <zeachco@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@tensorflow/tfjs-node-gpu": "^3.15.0",
    "three": "^0.139.2"
  },
  "devDependencies": {
    "@types/three": "^0.139.0",
    "nodemon": "^2.0.15",
    "parcel": "^2.4.1",
    "prettier": "^2.6.1",
    "serve": "^13.0.2"
  },
  "scripts": {
    "start": "parcel ./src/index.html",
    "build": "parcel build --dist-dir docs ./src/index.html",
    "release": "yarn build && git add docs/* && git commit -m 'deploy' && git push --set-upstream origin release"
  }
}
