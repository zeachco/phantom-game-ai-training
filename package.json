{
  "name": "nodium-ai",
  "version": "1.0.0",
  "author": "Olivier <zeachco@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@tensorflow/tfjs-node-gpu": "^3.15.0",
    "three": "^0.139.2"
  },
  "devDependencies": {
    "@types/three": "^0.139.0",
    "nodemon": "^2.0.15",
    "parcel": "^2.4.1",
    "prettier": "^2.6.1",
    "rimraf": "^3.0.2",
    "serve": "^13.0.2"
  },
  "scripts": {
    "start": "parcel ./src/index.html",
    "serve": "serve docs",
    "build": "parcel build --dist-dir docs ./src/index.html --public-url https://zeachco.github.io/phantom-game-ai-training/",
    "deploy": "yarn build && git add ./docs/* && git commit -m 'deploy' && git push origin main:release -f",
    "clean": "rimraf docs",
    "postdeploy": "yarn clean"
  }
}
